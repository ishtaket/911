<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev profile: allow plaintext HTTP only to the local backend.
  10.0.2.2 = host loopback as seen from the Android emulator.
  127.0.0.1 + localhost are for adb-reverse-port-forwarded setups.
  Everything else (production / staging) must be HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
