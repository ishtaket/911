[project]
name = "rescue911-backend"
version = "0.1.0"
description = "Rescue911 Israel OSINT/GeoINT missing-person backend"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["app/tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"
