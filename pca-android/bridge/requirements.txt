# Plain uvicorn (not uvicorn[standard]): the [standard] extra needs
# watchfiles, which has no aarch64-android wheel and won't compile under
# Termux without a Rust toolchain. The bridge runs without --reload.
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
